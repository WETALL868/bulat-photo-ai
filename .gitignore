# Собранное из исходников: пересобирается командой npm run build
seo-pages/
sitemap.xml
robots.txt
dist/

# Локальное
node_modules
var/
api/config-path.php
.DS_Store
Thumbs.db

# Стор синхронизации VTT: данные поставщика, а не исходники.
# Наполняется командой vtt/bin/vtt-sync.mjs и пересоздаётся синком.
vtt-data/

# Выгрузка поставщика: данные VTT, а не исходники. 5,4 МБ CSV и отчёт
# экспортёра кладутся сюда вручную и импортируются vtt-import-csv.
# Официальный WSDL при этом закоммичен — это спецификация, а не данные.
vtt-source/
vtt-data-mock/
assets/img/vtt/
